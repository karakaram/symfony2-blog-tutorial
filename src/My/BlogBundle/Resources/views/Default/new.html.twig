{% extends 'MyBlogBundle::layout.html.twig' %}

{% block body %}
<h1>Add Post</h1>
<form action="{{ path('blog_new_post') }}" method="post" {{ form_enctype(form) }} novalidate>
    {{ form_widget(form) }}
    <input type="submit" value="Save Post" />
</form>
{% endblock %}
